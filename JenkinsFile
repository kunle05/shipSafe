pipeline {
    agent any

    stages {
      stage("Install app dependencies") {
        steps {
          sh "npm install -y"
        }
      }

      stage("Run code") {
        steps {
          sh "npm run dev"
        }
      }
    }
}
